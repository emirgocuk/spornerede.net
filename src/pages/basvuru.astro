---
/**
 * basvuru.astro
 * Kulüp Başvuru Sayfası — /basvuru
 * Kulüplerin platforma kayıt için dolduracağı form.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import BasvuruFormSections from '../components/BasvuruFormSections.astro';
import { getAllCities, getAllDistrictsWithCities, getAllBranches } from '../lib/repositories/catalog';
import { BRANCHES } from '../data/mockData';

const success = Astro.url.searchParams.get('success') === '1';
const errorCode = Astro.url.searchParams.get('error') ?? '';
const errorMessages: Record<string, string> = {
  missing: 'Lütfen zorunlu alanları eksiksiz doldurun.',
  email: 'Lütfen geçerli bir e-posta adresi yazın.',
  server: 'Başvuru gönderilemedi. Lütfen tekrar deneyin.',
};
const errorMessage = errorMessages[errorCode] ?? '';

const [cityRows, districtRows, branchRows] = await Promise.all([
  getAllCities(),
  getAllDistrictsWithCities(),
  getAllBranches(),
]);
const CITIES = cityRows.map((c) => c.ad);
const DISTRICTS_BY_CITY = districtRows.reduce<Record<string, { ad: string; slug: string }[]>>((acc, district) => {
  if (!district.il) return acc;
  acc[district.il] ??= [];
  acc[district.il].push({ ad: district.ad, slug: district.slug });
  return acc;
}, {});
for (const il of Object.keys(DISTRICTS_BY_CITY)) {
  DISTRICTS_BY_CITY[il].sort((a, b) => a.ad.localeCompare(b.ad, 'tr'));
}

const rawBranchNames = [
  ...branchRows.map((b) => b.isim),
  ...BRANCHES.map((b) => b.isim),
  'Oryantiring',
  'Satranç',
];
const uniqueBranches = Array.from(new Set(rawBranchNames))
  .filter((name) => Boolean(name) && name !== 'Diğer')
  .sort((a, b) => a.localeCompare(b, 'tr'));

const bransListesi = [
  ...uniqueBranches,
  'Diğer',
];

const adimlar = [
  { num: '1', label: 'Kulüp' },
  { num: '2', label: 'Branş Sayısı' },
  { num: '3', label: 'İlanlar' },
];
---

<BaseLayout
  title="Kulübünü Ekle — Ücretsiz Başvuru Formu"
  description="SporNerede.net'e kulübünüzü veya kursunuzu ücretsiz ekleyin. Türkiye genelinde spor arayan ailelere ulaşın. Başvuru formu hızlı ve kolay."
  keywords="ücretsiz spor kulübü ekle, ücretsiz spor kursu ekle, spornerede başvuru, kulüp listesi"
>

  <div class="basvuru-page">

    <!-- ===== HERO ÜSTÜ ===== -->
    <div class="basvuru-hero">
      <div class="basvuru-hero-bg" aria-hidden="true">
        <div class="bh-orb bh-orb-1"></div>
        <div class="bh-orb bh-orb-2"></div>
      </div>
      <div class="container basvuru-hero-content">
        <div class="hero-left-content">
          <div class="basvuru-badge">🏆 Ücretsiz Platforma Katıl</div>
          <h1 class="basvuru-title">Kulübünüzü<br />Ücretsiz Listeleyin</h1>
          <p class="basvuru-desc">
            Kulüp bilgilerinizi girin, yayınlanacak branş sayınızı seçin ve ilanlarınızı oluşturun. Onay sonrası ilanlarınız ücretsiz olarak yayına alınır.
          </p>
          <!-- Adım göstergesi -->
          <div class="adimlar" aria-label="Form Adımları" role="list">
            {adimlar.map((adim, i) => (
              <div class="adim" role="listitem">
                <div class={`adim-num ${i === 0 ? 'adim-aktif' : ''}`} aria-current={i === 0 ? 'step' : undefined}>
                  {adim.num}
                </div>
                <span class="adim-label">{adim.label}</span>
              </div>
            ))}
          </div>
        </div>

        <div class="hero-right-content" aria-hidden="true"></div>
      </div>
    </div>

    <!-- ===== FORM ALANI ===== -->
    <div class="basvuru-form-wrapper">
      <div class="container">
        <div class="basvuru-grid">

          <!-- Form -->
          <main class="basvuru-form-card" id="main-content">

            {success ? (
              <!-- Başarı durumu -->
              <div class="success-state" role="alert" aria-live="polite">
                <div class="success-icon" aria-hidden="true">✅</div>
                <h2 class="success-title">Başvurunuz Alındı!</h2>
                <p class="success-desc">
                  Başvurunuzu inceleyeceğiz ve en kısa sürede ilanlarınız ücretsiz olarak yayınlanacaktır.
                </p>
                <a href="/" class="btn btn-primary">Ana Sayfaya Dön</a>
              </div>
            ) : (
              <form
                id="basvuru-form"
                method="post"
                action="/api/basvuru"
                novalidate
                aria-label="Kulüp Başvuru Formu"
              >

                <BasvuruFormSections
                  cities={CITIES}
                  bransListesi={bransListesi}
                  errorMessage={errorMessage || undefined}
                />

                <!-- Submit -->
                <div class="form-submit-area">
                  <p class="form-gizlilik">
                    Başvurarak <a href="/gizlilik" class="gizlilik-link">Gizlilik Politikası</a>'nı kabul etmiş olursunuz. Bilgileriniz üçüncü taraflarla paylaşılmaz.
                  </p>
                  <button type="submit" class="btn btn-primary btn-lg submit-btn" id="basvuru-submit-btn">
                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <polyline points="20 6 9 17 4 12"/>
                    </svg>
                    Ücretsiz Başvuruyu Gönder
                  </button>
                </div>

                <div class="form-contact-card">
                  <h3 class="form-contact-title">Sorularınız mı var?</h3>
                  <p class="form-contact-text">Başvuru süreciyle ilgili yardım için bize ulaşın:</p>
                  <div class="form-contact-actions">
                    <a
                      href="https://wa.me/905362982218"
                      class="form-contact-action form-contact-action--whatsapp"
                      target="_blank"
                      rel="noopener noreferrer"
                      aria-label="WhatsApp ile doğrudan mesaj gönder"
                    >
                      <img src="/icons/whatsapp.svg" alt="" aria-hidden="true" width="16" height="16" />
                      WhatsApp'tan Ulaş
                    </a>
                    <a
                      href="mailto:info@spornerede.net"
                      class="form-contact-action form-contact-action--email"
                      aria-label="E-posta ile iletişime geç"
                    >
                      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                        <polyline points="22,6 12,13 2,6"/>
                      </svg>
                      info@spornerede.net
                    </a>
                  </div>
                </div>

              </form>
            )}
          </main>

        </div>
      </div>
    </div>

  </div>

  <script id="district-data" type="application/json" set:html={JSON.stringify(DISTRICTS_BY_CITY)}></script>

  <script is:inline>
    const citySelect = document.getElementById('kulup-il');
    const districtSelect = document.getElementById('kulup-ilce');
    const districtDataEl = document.getElementById('district-data');
    const districtsByCity = districtDataEl ? JSON.parse(districtDataEl.textContent || '{}') : {};
    const formDraftKey = 'spornerede:basvuru-form-draft:v1';

    function fillDistricts(cityName, selectedDistrict = '') {
      if (!(districtSelect instanceof HTMLSelectElement)) return;
      districtSelect.innerHTML = '';

      const districts = districtsByCity[cityName] || [];
      const placeholder = document.createElement('option');
      placeholder.value = '';
      placeholder.textContent = cityName ? (districts.length ? 'İlçe seçin...' : 'Bu il için ilçe bulunamadı') : 'Önce il seçin...';
      districtSelect.appendChild(placeholder);

      districts.forEach((district) => {
        const option = document.createElement('option');
        option.value = district.ad;
        option.textContent = district.ad;
        option.selected = district.ad === selectedDistrict;
        districtSelect.appendChild(option);
      });
    }

    if (citySelect instanceof HTMLSelectElement) {
      fillDistricts(citySelect.value);
      citySelect.addEventListener('change', () => fillDistricts(citySelect.value));
    }

    const branchRoot = document.getElementById('branch-listings-root');
    const branchTemplate = document.getElementById('branch-listing-template');
    const branchFieldKeys = ['brans', 'yasAraligi', 'aidat'];

    function configureBranchSection(section, index) {
      if (!(section instanceof HTMLElement)) return;
      const title = section.querySelector('.branch-listing-title-text');
      if (title) title.textContent = `${index + 1}. Branş Bilgileri (İlan)`;
      const numEl = section.querySelector('.branch-listing-num');
      if (numEl) numEl.textContent = String(index + 3);
      branchFieldKeys.forEach((field) => {
        const el = section.querySelector(`[data-field="${field}"]`);
        if (el instanceof HTMLInputElement || el instanceof HTMLSelectElement || el instanceof HTMLTextAreaElement) {
          el.name = `ilan_${index}_${field}`;
        }
      });
    }

    function renderBranchSections(count) {
      if (!branchRoot || !branchTemplate) return;
      const source = branchTemplate.content?.firstElementChild;
      if (!source) return;
      const fragment = document.createDocumentFragment();
      for (let i = 0; i < count; i++) {
        const section = source.cloneNode(true);
        configureBranchSection(section, i);
        fragment.appendChild(section);
      }
      branchRoot.replaceChildren(fragment);
    }

    const kulupInput = document.getElementById('kulup-ad');
    const branchCountHidden = document.getElementById('brans-sayisi-hidden');
    const branchCountInputs = Array.from(document.querySelectorAll('[data-branch-input]'));

    function collectBranchDraft() {
      return Array.from(branchRoot?.querySelectorAll('[data-branch-section]') || []).map((section) => ({
        brans: section.querySelector('[data-field="brans"]')?.value || '',
        yasAraligi: section.querySelector('[data-field="yasAraligi"]')?.value || '',
        aidat: section.querySelector('[data-field="aidat"]')?.value || '',
      }));
    }

    function applyBranchDraft(ilanlar) {
      if (!Array.isArray(ilanlar)) return;
      Array.from(branchRoot?.querySelectorAll('[data-branch-section]') || []).forEach((section, index) => {
        const ilan = ilanlar[index] || {};
        branchFieldKeys.forEach((field) => {
          const el = section.querySelector(`[data-field="${field}"]`);
          if (el instanceof HTMLInputElement || el instanceof HTMLSelectElement || el instanceof HTMLTextAreaElement) {
            el.value = ilan[field] || '';
          }
        });
      });
    }

    function getDraftValue(id) {
      const el = document.getElementById(id);
      return el instanceof HTMLInputElement || el instanceof HTMLSelectElement || el instanceof HTMLTextAreaElement
        ? el.value
        : '';
    }

    function collectFormDraft() {
      return {
        bransSayisi: branchCountHidden instanceof HTMLInputElement ? branchCountHidden.value : '1',
        kulupad: getDraftValue('kulup-ad'),
        il: getDraftValue('kulup-il'),
        ilce: getDraftValue('kulup-ilce'),
        adres: getDraftValue('kulup-adres'),
        yetkili: getDraftValue('yetkili-ad'),
        telefon: getDraftValue('yetkili-telefon'),
        email: getDraftValue('yetkili-email'),
        ilanlar: collectBranchDraft(),
      };
    }

    function saveFormDraft() {
      try {
        window.localStorage.setItem(formDraftKey, JSON.stringify(collectFormDraft()));
      } catch {
        // LocalStorage kapalıysa form normal şekilde çalışmaya devam eder.
      }
    }

    function readFormDraft() {
      try {
        const raw = window.localStorage.getItem(formDraftKey);
        return raw ? JSON.parse(raw) : null;
      } catch {
        return null;
      }
    }

    function applyFormDraft(draft) {
      if (!draft || typeof draft !== 'object') return;

      const simpleFields = {
        'kulup-ad': draft.kulupad,
        'kulup-adres': draft.adres,
        'yetkili-ad': draft.yetkili,
        'yetkili-telefon': draft.telefon,
        'yetkili-email': draft.email,
      };
      Object.entries(simpleFields).forEach(([id, value]) => {
        const el = document.getElementById(id);
        if (el instanceof HTMLInputElement || el instanceof HTMLTextAreaElement) {
          el.value = value || '';
        }
      });

      if (citySelect instanceof HTMLSelectElement) {
        citySelect.value = draft.il || '';
        fillDistricts(citySelect.value, draft.ilce || '');
      }

      const branchCount = Number(draft.bransSayisi || draft.ilanlar?.length || 1);
      syncBranchCount(branchCount, Array.isArray(draft.ilanlar) ? draft.ilanlar : []);
    }

    function syncBranchCount(nextValue, preservedIlanlar = collectBranchDraft()) {
      const count = Number.isFinite(nextValue) ? Math.max(1, Math.min(1, Math.floor(nextValue))) : 1;
      if (branchCountHidden instanceof HTMLInputElement) {
        branchCountHidden.value = String(count);
      }
      branchCountInputs.forEach((input) => {
        if (input instanceof HTMLInputElement && input.value !== String(count)) {
          input.value = String(count);
        }
      });
      renderBranchSections(count);
      applyBranchDraft(preservedIlanlar);
    }

    kulupInput?.addEventListener('input', () => {
      saveFormDraft();
    });
    branchCountInputs.forEach((input) => {
      input.addEventListener('input', () => {
        syncBranchCount(Number(input.value));
        saveFormDraft();
      });
      input.addEventListener('click', (event) => event.stopPropagation());
      input.addEventListener('keydown', (event) => event.stopPropagation());
    });
    syncBranchCount(1);
    if (new URLSearchParams(window.location.search).has('error')) {
      applyFormDraft(readFormDraft());
    }
    document.getElementById('basvuru-form')?.addEventListener('input', saveFormDraft);
    document.getElementById('basvuru-form')?.addEventListener('change', saveFormDraft);
    document.getElementById('basvuru-form')?.addEventListener('submit', saveFormDraft);
    if (new URLSearchParams(window.location.search).get('success') === '1') {
      window.localStorage.removeItem(formDraftKey);
      window.addEventListener('load', function () {
        if (typeof window.spornereedeTrack === 'function') {
          window.spornereedeTrack('application_success');
        }
      }, { once: true });
    }
  </script>

</BaseLayout>


<style>
  /* ===== GENEL SAYFA ===== */
  .basvuru-page {
    min-height: 100vh;
    background: #F8F9FA;
  }

  /* ===== HERO ===== */
  .basvuru-hero {
    position: relative;
    background: linear-gradient(135deg, #0D0D0D 0%, #1a0002 60%, #2d0005 100%);
    color: #ffffff;
    padding: 4rem 0 3rem;
    overflow: hidden;
  }

  .basvuru-hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .bh-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
  }

  .bh-orb-1 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(227,10,23,0.2) 0%, transparent 70%);
    top: -100px;
    right: 5%;
  }

  .bh-orb-2 {
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(255,255,255,0.04) 0%, transparent 70%);
    bottom: 0;
    left: 10%;
  }

  .basvuru-hero-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-width: 840px;
  }

  @media (min-width: 900px) {
    .basvuru-hero-content {
      display: flex;
    }
  }

  .hero-left-content {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .hero-right-content {
    display: flex;
    justify-content: center;
  }

  @media (min-width: 900px) {
    .hero-right-content {
      justify-content: flex-end;
    }
  }

  .hero-feature-card-glass {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 1.25rem;
    padding: 2rem;
    width: 100%;
    max-width: 400px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  }

  .hero-feature-title {
    font-size: 1.25rem;
    font-weight: 800;
    color: #ffffff;
    margin: 0 0 1.25rem 0;
  }

  .hero-feature-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .hero-feature-list li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.95rem;
    font-weight: 500;
  }

  .basvuru-badge {
    display: inline-block;
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.15);
    color: rgba(255,255,255,0.85);
    font-size: 0.8rem;
    font-weight: 700;
    padding: 0.35rem 0.875rem;
    border-radius: 9999px;
    width: fit-content;
    backdrop-filter: blur(8px);
  }

  .basvuru-title {
    font-size: clamp(1.75rem, 5vw, 3rem);
    font-weight: 900;
    color: #ffffff;
    letter-spacing: -0.03em;
    line-height: 1.15;
  }

  .basvuru-desc {
    font-size: 1rem;
    color: rgba(255,255,255,0.6);
    max-width: 480px;
  }

  /* ===== ADIMLAR ===== */
  .adimlar {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 0.5rem;
  }

  .adim {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .adim:not(:last-child)::after {
    content: '→';
    color: rgba(255,255,255,0.25);
    font-size: 0.8rem;
    margin-left: 0.25rem;
  }

  .adim-num {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: rgba(255,255,255,0.1);
    border: 1px solid rgba(255,255,255,0.2);
    color: rgba(255,255,255,0.5);
    font-size: 0.78rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .adim-aktif {
    background: #E30A17;
    border-color: #E30A17;
    color: #ffffff;
  }

  .adim-label {
    font-size: 0.78rem;
    color: rgba(255,255,255,0.55);
    font-weight: 600;
  }

  /* ===== FORM WRAPPER ===== */
  .basvuru-form-wrapper {
    padding: 3rem 0 5rem;
  }

  .basvuru-grid {
    display: flex;
    justify-content: center;
  }

  /* ===== FORM KART ===== */
  .basvuru-form-card {
    background: #ffffff;
    border-radius: 1.25rem;
    border: 1px solid #E9ECEF;
    padding: 2.5rem;
    box-shadow: 0 4px 24px rgba(0,0,0,0.07);
    width: 100%;
    max-width: 1120px;
  }

  /* ===== FORM SECTION ===== */
  .form-section {
    margin-bottom: 2.5rem;
    padding-bottom: 2.5rem;
    border-bottom: 1px solid #F1F3F5;
  }

  .form-section:last-of-type {
    border-bottom: none;
    margin-bottom: 1.5rem;
    padding-bottom: 0;
  }

  .form-section-title {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 1.05rem;
    font-weight: 800;
    color: #212529;
    margin-bottom: 1.75rem;
  }

  .form-section-num {
    width: 30px;
    height: 30px;
    background: #E30A17;
    color: #ffffff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.82rem;
    font-weight: 800;
    flex-shrink: 0;
  }

  /* ===== FORM ROWS ===== */
  .form-row {
    margin-bottom: 1.25rem;
  }

  .form-row-2col {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }

  @media (min-width: 640px) {
    .form-row-2col { grid-template-columns: 1fr 1fr; }
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    position: relative;
    transition: all 0.3s ease;
  }

  .form-group:focus-within .form-label {
    color: #E30A17;
    transform: translateX(2px);
  }

  .form-label {
    font-size: 0.88rem;
    font-weight: 700;
    color: #495057;
    transition: all 0.3s ease;
    display: inline-block;
  }

  .form-input,
  .form-select,
  .form-textarea {
    width: 100%;
    padding: 0.85rem 1.1rem;
    border: 2px solid #E9ECEF;
    border-radius: 0.85rem;
    background: #ffffff;
    font-family: inherit;
    font-size: 0.95rem;
    color: #212529;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: #ADB5BD;
    transition: opacity 0.3s ease;
  }

  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: #E30A17;
    background: #fffafa;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(227, 10, 23, 0.08), 0 0 0 4px rgba(227, 10, 23, 0.1);
  }

  .form-input:focus::placeholder,
  .form-textarea:focus::placeholder {
    opacity: 0.5;
  }

  /* Icı dolu ama odakta degilken (valid) hafif vurgu */
  .form-input:not(:placeholder-shown):not(:focus),
  .form-textarea:not(:placeholder-shown):not(:focus) {
    border-color: #cbd5e1;
    background: #f8fafc;
  }

  .required {
    color: #E30A17;
    font-size: 1rem;
    line-height: 1;
  }

  .form-alert {
    padding: 0.9rem 1rem;
    border-radius: 0.85rem;
    font-weight: 700;
    line-height: 1.5;
    margin-bottom: 1.25rem;
  }

  .form-alert--error {
    color: #991b1b;
    background: #fef2f2;
    border: 1px solid #fecaca;
  }

  .membership-empty {
    grid-column: 1 / -1;
    margin-bottom: 0;
  }

  .branch-listings-root {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .branch-listing-section {
    border: 1px solid #e8eaed;
    border-radius: 1rem;
    padding: 1.25rem 1.5rem;
    background: #fff;
  }

  .branch-listing-help {
    color: #6b7280;
    font-size: 0.9rem;
    margin: 0 0 1rem;
  }

  .payment-section-last {
    margin-top: 0.5rem;
  }

  .paket-subtitle {
    margin: 0 0 1.5rem;
    color: #495057;
    font-size: 1.05rem;
    font-weight: 500;
  }

  .membership-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
    align-items: stretch;
  }

  @media (min-width: 768px) {
    .membership-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .membership-card {
    position: relative;
    cursor: pointer;
    display: block;
  }

  .membership-radio {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  .membership-card-content {
    position: relative;
    border: 2px solid #E9ECEF;
    border-radius: 1.25rem;
    padding: 1.75rem 1.5rem;
    background: #ffffff;
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
    display: flex;
    flex-direction: column;
    min-height: 100%;
    z-index: 1;
    overflow: hidden;
  }

  .membership-card:hover .membership-card-content {
    transform: translateY(-4px);
    border-color: #dee2e6;
    box-shadow: 0 12px 24px rgba(0,0,0,0.06);
  }

  .membership-radio:checked + .membership-card-content {
    border-color: #E30A17;
    background: #ffffff;
    box-shadow: 0 12px 30px rgba(227,10,23,0.15), 0 0 0 1px #E30A17;
  }
  
  .membership-radio:checked + .membership-card-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: #E30A17;
  }


  .membership-header {
    margin-bottom: 1.25rem;
  }

  .membership-name {
    font-size: 1.25rem;
    font-weight: 900;
    color: #212529;
    margin: 0 0 0.25rem;
  }

  .membership-period {
    font-size: 0.85rem;
    color: #6C757D;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .membership-price-area {
    margin-bottom: 1.5rem;
  }

  .membership-price {
    font-size: 2.25rem;
    font-weight: 900;
    color: #111827;
    line-height: 1;
    letter-spacing: -0.02em;
    display: flex;
    align-items: baseline;
    gap: 0.25rem;
  }
  
  .membership-radio:checked + .membership-card-content .membership-price {
    color: #E30A17;
  }
  
  .membership-price .currency {
    font-size: 1.15rem;
    font-weight: 700;
    color: #6C757D;
  }

  .membership-monthly {
    font-size: 0.9rem;
    color: #16A34A;
    font-weight: 700;
    margin-top: 0.5rem;
    background: #DCFCE7;
    display: inline-block;
    padding: 0.2rem 0.6rem;
    border-radius: 0.35rem;
  }

  .membership-discount-badge {
    display: inline-block;
    margin-top: 0.5rem;
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
    font-size: 0.78rem;
    font-weight: 700;
    color: #9a3412;
    background: #ffedd5;
    border: 1px solid #fed7aa;
  }
  
  .membership-monthly-placeholder {
    height: 1.65rem;
    margin-top: 0.5rem;
  }
  
  .membership-divider {
    height: 1px;
    background: #E9ECEF;
    margin: 0 0 1.5rem;
  }
  
  .membership-features {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
    flex-grow: 1;
  }
  
  .membership-features li {
    font-size: 0.8rem;
    color: #495057;
    font-weight: 500;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    gap: 0.5rem;
    line-height: 1.4;
    text-align: left;
  }
  
  .feature-icon {
    color: #E30A17;
    flex-shrink: 0;
    width: 16px;
    height: 16px;
    display: inline-block;
  }
  
  .membership-select-btn {
    width: 100%;
    text-align: center;
    padding: 0.75rem;
    border-radius: 0.75rem;
    font-weight: 700;
    font-size: 0.95rem;
    background: linear-gradient(180deg, #fff1f2 0%, #ffe4e6 100%);
    color: #b91c1c;
    border: 1px solid #fecdd3;
    transition: all 0.2s ease;
  }

  .membership-card:hover .membership-select-btn {
    border-color: #fda4af;
    background: linear-gradient(180deg, #ffe4e6 0%, #fecdd3 100%);
  }
  
  .membership-radio:checked + .membership-card-content .membership-select-btn {
    background: #E30A17;
    color: #ffffff;
    border-color: #E30A17;
  }
  
  .payment-box {
    margin-top: 1.5rem;
    padding: 1.25rem;
    border: 1px solid #FECACA;
    border-radius: 1rem;
    background: linear-gradient(180deg, #fff7f7 0%, #ffffff 100%);
    display: block;
  }

  @keyframes slideDownPayment {
    from {
      opacity: 0;
      transform: translateY(-15px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .payment-title {
    margin: 0 0 0.35rem;
    font-size: 1.05rem;
    font-weight: 800;
    color: #7f1d1d;
  }

  .payment-desc {
    margin: 0 0 0.85rem;
    font-size: 0.88rem;
    color: #7f1d1d;
  }

  .payment-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.65rem;
    margin-bottom: 0.9rem;
  }

  @media (min-width: 640px) {
    .payment-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .payment-item {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    padding: 0.65rem 0.75rem;
    border-radius: 0.6rem;
    border: 1px solid #FEE2E2;
    background: #ffffff;
  }

  .payment-item--iban {
    grid-column: 1 / -1;
  }

  .payment-label {
    font-size: 0.72rem;
    text-transform: uppercase;
    color: #b91c1c;
    font-weight: 700;
    letter-spacing: 0.04em;
  }

  .payment-item strong {
    font-size: 0.92rem;
    color: #111827;
    word-break: break-word;
  }

  #dyn-payment-total.payment-total-pending {
    color: #9ca3af;
    font-weight: 600;
  }

  .payment-steps {
    margin: 0 0 0.9rem;
    padding-left: 1.1rem;
    color: #374151;
    font-size: 0.85rem;
    line-height: 1.5;
  }

  .payment-reference {
    display: grid;
    gap: 0.4rem;
    margin-bottom: 0.9rem;
  }

  .payment-reference label {
    font-size: 0.78rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: #b91c1c;
  }

  .payment-reference input {
    width: 100%;
    border: 1px solid #fecaca;
    border-radius: 0.6rem;
    padding: 0.7rem 0.8rem;
    background: #fff;
    color: #1f2937;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .helper-text {
    font-size: 0.78rem;
    color: #6b7280;
  }

  .membership-count-caption {
    margin: 0 0 1.2rem;
    font-size: 0.85rem;
    color: #4b5563;
    line-height: 1.5;
    flex-grow: 1;
  }

  .membership-branch-global {
    margin-top: 1.25rem;
    padding: 1.1rem 1.25rem;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    background: #fff;
    display: grid;
    gap: 0.5rem;
    max-width: 420px;
  }

  .membership-branch-global label {
    font-size: 0.78rem;
    font-weight: 700;
    color: #4b5563;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .membership-branch-help {
    margin: 0;
    font-size: 0.85rem;
    color: #6b7280;
    line-height: 1.45;
  }

  .membership-branch-input {
    padding: 0.7rem 0.9rem;
  }

  .membership-branch-input--global {
    max-width: 140px;
  }

  .payment-confirm {
    display: flex;
    align-items: flex-start;
    gap: 0.6rem;
    font-size: 0.85rem;
    color: #374151;
    margin-top: 0.75rem;
    cursor: pointer;
    user-select: none;
  }

  .sr-only-checkbox {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }

  .custom-checkbox {
    width: 20px;
    height: 20px;
    border-radius: 6px;
    border: 2px solid #cbd5e1;
    background: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-top: 0.15rem;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .check-icon {
    width: 12px;
    height: 12px;
    color: #ffffff;
    opacity: 0;
    transform: scale(0.5);
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .sr-only-checkbox:checked + .custom-checkbox {
    background: #E30A17;
    border-color: #E30A17;
    animation: checkboxPop 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .sr-only-checkbox:checked + .custom-checkbox .check-icon {
    opacity: 1;
    transform: scale(1);
  }

  .sr-only-checkbox:focus-visible + .custom-checkbox {
    box-shadow: 0 0 0 3px rgba(227, 10, 23, 0.2);
  }

  @keyframes checkboxPop {
    0% { transform: scale(1); }
    50% { transform: scale(1.15); }
    100% { transform: scale(1); }
  }

  /* ===== SUBMIT ALANI ===== */
  .form-submit-area {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
    text-align: center;
  }

  .form-gizlilik {
    font-size: 0.8rem;
    color: #6C757D;
    line-height: 1.5;
    text-align: center;
  }

  .gizlilik-link {
    color: #E30A17;
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .submit-btn {
    align-self: center;
    justify-content: center;
    min-width: 280px;
  }

  @media (min-width: 640px) {
    .submit-btn { align-self: center; }
  }

  /* ===== BAŞARI DURUMU ===== */
  .success-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 3rem 1rem;
    gap: 1.25rem;
  }

  .success-icon {
    font-size: 3.5rem;
  }

  .success-title {
    font-size: 1.75rem;
    font-weight: 900;
    color: #212529;
  }

  .success-desc {
    font-size: 0.95rem;
    color: #6C757D;
    max-width: 400px;
    line-height: 1.6;
  }

  /* ===== SIDEBAR ===== */
  .basvuru-sidebar {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .sidebar-card {
    background: #ffffff;
    border: 1px solid #E9ECEF;
    border-radius: 1rem;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .sidebar-icon {
    font-size: 2rem;
  }

  .sidebar-title {
    font-size: 1rem;
    font-weight: 800;
    color: #212529;
  }

  .sidebar-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }

  .sidebar-list li {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    font-size: 0.87rem;
    color: #495057;
    font-weight: 500;
  }

  .sidebar-iletisim {
    background: #F8F9FA;
  }

  .sidebar-subtitle {
    font-size: 0.88rem;
    font-weight: 700;
    color: #212529;
  }

  .sidebar-contact-text {
    font-size: 0.82rem;
    color: #6C757D;
  }

  .sidebar-email {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.88rem;
    font-weight: 700;
    color: #E30A17;
    text-decoration: none;
    transition: opacity 0.15s ease;
  }

  .sidebar-email:hover {
    opacity: 0.8;
  }
  /* ===== CONTACT KART ===== */
  .form-contact-card {
    margin-top: 3.5rem;
    padding-top: 2rem;
    border-top: 1px dashed #E9ECEF;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    align-items: center;
    text-align: center;
  }

  .form-contact-title {
    font-size: 1.15rem;
    font-weight: 800;
    color: #212529;
    margin: 0;
  }

  .form-contact-text {
    font-size: 0.95rem;
    color: #6C757D;
    margin: 0;
  }

  .form-contact-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 0.65rem;
    margin-top: 0.5rem;
  }

  .form-contact-action {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1rem;
    font-weight: 700;
    text-decoration: none;
    padding: 0.72rem 1.2rem;
    border-radius: 0.75rem;
    border: 1px solid transparent;
    transition: all 0.2s ease;
  }

  .form-contact-action--whatsapp {
    background: #25D366;
    color: #ffffff;
    border-color: rgba(255, 255, 255, 0.25);
  }

  .form-contact-action--whatsapp:hover {
    background: #1fb957;
    color: #ffffff;
    border-color: #1fb957;
    transform: translateY(-1px);
    box-shadow: 0 10px 24px rgba(37, 211, 102, 0.3);
  }

  .form-contact-action--email {
    background: #fff5f5;
    color: #E30A17;
    border-color: #fee2e2;
  }

  .form-contact-action--email:hover {
    background: #E30A17;
    color: #ffffff;
    border-color: #E30A17;
    transform: translateY(-1px);
  }

</style>
