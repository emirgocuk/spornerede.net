---
/** Eski üye girişi URL'si — gizli admin girişine yönlendirir. */
const target = new URL('/admin', Astro.url);
Astro.url.searchParams.forEach((value, key) => {
  target.searchParams.set(key, value);
});
return Astro.redirect(target.pathname + target.search, 308);
---
